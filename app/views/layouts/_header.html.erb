<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle collapsed" data-target="#navbar" data-toggle="collapse" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span></button>
        <%= link_to "OAQ", root_path, :class => "navbar-brand" %>
    </div>

    <div class="navbar-collapse collapse" id="navbar">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:action] == 'home' %>">
          <%= link_to "Home", root_path %>
        </li>

        <% if user_signed_in? %>

          <li class="">
            <%= link_to "Institutions", institutions_path %>
          </li>

          <li class="">
            <%= link_to "Users", users_path %>
          </li>

          <!-- questionnaires -->
          <li class="dropdown <%= "active" if (params[:controller] == 'questionnaires' || params[:controller] == 'questions' ) %>">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Questionnaires
            <span class="caret"></span></a>

            <ul class="dropdown-menu">
              <!-- questionnaires -->
              <li>
                <%= link_to "Questionnaires", questionnaires_path %>
              </li>

              <!-- questions -->
              <li>
                <%= link_to "Questions", questions_path %>
              </li>

            </ul>
          </li>

          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Profile
            <span class="caret"></span></a>

            <ul class="dropdown-menu">
              <li>
                <%= link_to "Invite Users", new_user_invitation_path %>
              </li>

              <li>
                <%= link_to "Update Profile", edit_user_registration_path %>
              </li>

              <li>
                <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
              </li>
            </ul>
          </li>
        <% else %>

          <li class="">
            <%= link_to "Sign", new_user_session_path %>
          </li>

        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
